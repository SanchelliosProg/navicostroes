<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:map="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="store"
            type="com.tstasks.sanchellios.navicostores.binders.StoreBinder"/>
    </data>

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingBottom="@dimen/activity_vertical_margin"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:paddingTop="@dimen/activity_vertical_margin"
        tools:context="com.tstasks.sanchellios.navicostores.DetailsActivity">

        <fragment
            xmlns:android="http://schemas.android.com/apk/res/android"
            android:id="@+id/map"
            android:layout_width="match_parent"
            android:layout_height="150dp"
            android:name="com.google.android.gms.maps.MapFragment"/>

        <TextView
            android:id="@+id/store_name_txt_view_detailed"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            style="@style/TextLabel"
            android:text="@{store.name}"/>

        <Button
            android:id="@+id/num_of_instruments_button"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@{@string/in_the_store + ' ' + store.numberOfInstruments + ' ' + @plurals/instrumentsAvailable(store.numberOfInstruments)}"/>

    </LinearLayout>
</layout>

